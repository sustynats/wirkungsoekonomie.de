name: Hosting cost guard

on:
  pull_request:
    paths:
      - "AGENTS.md"
      - "ops/hosting-cost-policy.json"
      - "scripts/ops/check-hosting-cost-guard.mjs"
      - "scripts/ops/check-vercel-release-budget.mjs"
      - "scripts/ops/check-vercel-account-cost-guard.mjs"
      - "ops/vercel-project-baseline.json"
      - "ops/vercel-build-ledger.jsonl"
      - "scripts/ops/reserve-vercel-build-slot.mjs"
      - "woek-parlament-app/vercel.json"
      - ".github/workflows/hosting-cost-guard.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm run check:hosting-cost
