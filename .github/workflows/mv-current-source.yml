name: Mecklenburg-Vorpommern current-source completion

on:
  pull_request:
    paths:
      - "tools/validate_mv_current_source_register.py"
      - "woek-parlament-app/app/components/StateProgrammeSourceRegister.tsx"
      - "woek-parlament-app/app/laender/[slug]/wahl/page.tsx"
      - "woek-parlament-app/data/state-programmes/current-source-registers/mecklenburg-vorpommern-2026.json"
      - "woek-parlament-app/data/state-programmes/current-source-registers/mecklenburg-vorpommern-2026-v2.json"
      - "woek-parlament-app/lib/states/public-content.ts"
      - "woek-parlament-app/scripts/quality/check-mv-current-source.ts"
      - "woek-parlament-app/scripts/quality/check-state-publication.ts"
      - "woek-parlament-app/tests/state-public-content.test.ts"
      - "woek-parlament-app/package.json"
      - ".github/workflows/mv-current-source.yml"
  workflow_dispatch:

permissions:
  contents: read
  issues: read

concurrency:
  group: mv-current-source-${{ github.ref }}
  cancel-in-progress: true

jobs:
  source-register:
    name: Final 19-list field, source maturity and GitHub pins
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Validate deterministic register and pinned source handoffs
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python3 tools/validate_mv_current_source_register.py --check-github

  public-projection:
    name: MV current-source public projection
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: woek-parlament-app
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: woek-parlament-app/package-lock.json
      - run: npm ci
      - run: npm run check:mv-current-source
      - run: npm run check:state-publication
      - run: npm run typecheck
