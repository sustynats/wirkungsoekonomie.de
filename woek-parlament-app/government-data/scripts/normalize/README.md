# Normalisierung

Die Normalisierung ist in Version 1.0 bewusst Teil des reproduzierbaren
Ingest-Laufs. Originaldaten bleiben unverändert im Raw-Layer; normalisierte
SourceEvents werden ausschließlich zusätzlich erzeugt.
